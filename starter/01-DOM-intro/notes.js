Nodes : elemets, text,

document.getElemendbytagnames renvoie un tableau alors que
document.getElemendById renvoie un objets

document.getElemendsByTagnames.style ne repons que pour les tableaux !!!!!!

document.querySelector ${".lool"} retourne le premier element qui s'appelle lool si il yen a pas il envoie null

document.querySelectorall retourne tous les elemets qu is'appelle lol sous forme de tableau !!!!!!!

voir PowerPoint js cours dom

document.getElemendsByTagnames.('p')[1] renvoie une case du tableau est peut doncs'utiliser comme un ByID

on peut utiliser next sibling ou parentNode ou encore prevouissibling

console.log(document.getElemendById ('#tott'));
ne fonctionne pas

parenthes = methode si il y a pas de parenthese ce qu'on cache une propriété

firstchild va selectionnner le retour çà la ligne il faut donc mettre un
firstelementchild
